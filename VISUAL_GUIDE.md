# 🎨 VİZÜEL DEPLOYMENT REHBERI

Bu dokümanda deployment adımlarını Visual olarak göstereceğiz.

---

## 📊 SISTEM MİMARİSİ

```
                    İNTERNET
                       │
        ┌──────────────┴──────────────┐
        │                             │
        ↓                             ↓
    
    🔵 VERCEL                     🟡 RENDER
    ┌─────────────────┐          ┌──────────────────┐
    │   FRONTEND      │          │    BACKEND       │
    │   (React)       │          │  (Node.js)       │
    │   :443 HTTPS    │◄─────────│  :10000          │
    │                 │ GraphQL   │                  │
    │ • Login Page    │          │ • Apollo Server  │
    │ • Dashboard     │          │ • JWT Auth       │
    │ • Documents     │          │ • Admin Panel    │
    │ • Edit Page     │          │ • API Endpoints  │
    └─────────────────┘          └────────┬─────────┘
                                          │
                                          │ SQL
                                          ↓
                                  ┌──────────────────┐
                                  │   DATABASE       │
                                  │  PostgreSQL      │
                                  │  privacy_policy  │
                                  │                  │
                                  │ • Users          │
                                  │ • Documents      │
                                  │ • Questions      │
                                  └──────────────────┘
```

---

## 🔄 DEPLOYMENT WORKFLOW

```
┌─────────────────────────────────────────────────┐
│  1. GitHub'a Push Et                             │
│     git push origin main                         │
└────────────────┬────────────────────────────────┘
                 │
       ┌─────────┴─────────┐
       │                   │
       ↓                   ↓
┌────────────┐      ┌────────────┐
│  Render    │      │  Vercel    │
│ Webhook    │      │ Webhook    │
│ Triggered  │      │ Triggered  │
└─────┬──────┘      └──────┬─────┘
      │                    │
      ↓                    ↓
┌────────────────────────────────────────────┐
│  2. Automatic Build & Tests                │
│     • npm install                          │
│     • npm run build                        │
│     • prisma generate (backend)            │
│     • Tests run                            │
└────────────┬───────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────┐
│  3. Deployment                              │
│     • Upload build                         │
│     • Start services                       │
│     • Run migrations (backend)             │
│     • Health checks                        │
└────────────┬───────────────────────────────┘
             │
             ↓
┌────────────────────────────────────────────┐
│  4. Live on Internet!                       │
│     ✅ Backend: https://backend.onrender.. │
│     ✅ Frontend: https://app.vercel.app    │
└────────────────────────────────────────────┘
```

---

## 👥 RENDER & VERCEL SETUP FLOW

### RENDER SETUP (Sağ tarafta)

```
┌─────────────────────────────────────────────┐
│  Step 1: Create PostgreSQL Database         │
│                                              │
│  render.com → New → PostgreSQL              │
│  ├─ Name: privacy-policy-db                 │
│  ├─ Database: privacy_policy                │
│  ├─ User: postgres                          │
│  ├─ Password: [Strong!]                     │
│  └─ Region: EU - Frankfurt                  │
│                                              │
│  ⏳ Wait 5-10 minutes                        │
│  ✅ Copy Internal Database URL              │
└─────────────────────────────────────────────┘
                    │
                    ↓
┌─────────────────────────────────────────────┐
│  Step 2: Create Web Service (Backend)       │
│                                              │
│  render.com → New → Web Service             │
│  ├─ Connect GitHub                          │
│  ├─ Select repo                             │
│  ├─ Name: privacy-policy-backend            │
│  ├─ Root Directory: backend                 │
│  ├─ Build Cmd: npm install &&...            │
│  ├─ Start Cmd: npm start                    │
│  └─ Add Environment Variables:              │
│     ├─ DATABASE_URL                         │
│     ├─ JWT_SECRET                           │
│     ├─ GOOGLE_API_KEY                       │
│     └─ FRONTEND_URL                         │
│                                              │
│  ⏳ Wait 5 minutes for build                 │
│  ✅ Status should be "Live"                 │
└─────────────────────────────────────────────┘
```

### VERCEL SETUP (Sol tarafta)

```
┌─────────────────────────────────────────────┐
│  Step 1: Import & Configure                 │
│                                              │
│  vercel.com → Import Project                │
│  ├─ Select GitHub repo                      │
│  ├─ Framework: Create React App             │
│  ├─ Root Directory: frontend                │
│  ├─ Build Command: npm run build            │
│  └─ Add Environment Variables:              │
│     └─ REACT_APP_GRAPHQL_ENDPOINT           │
│                                              │
│  ⏳ Wait 3-5 minutes for build              │
│  ✅ See "Deployment Complete"               │
└─────────────────────────────────────────────┘
```

---

## 📋 ENVIRONMENT VARIABLES MAPPING

```
┌────────────────────────────────────────────────┐
│         ENVIRONMENT VARIABLES                   │
└────────────────────────────────────────────────┘

GitHub Push
    │
    ├─────────────────────┬──────────────────┐
    │                     │                  │
    ↓                     ↓                  ↓
  Render              Render             Vercel
  
┌─────────────────┐ ┌──────────────┐ ┌──────────────┐
│ BACKEND (Node)  │ │ DATABASE     │ │ FRONTEND     │
├─────────────────┤ ├──────────────┤ ├──────────────┤
│ NODE_ENV        │ │ DATABASE_URL │ │ REACT_APP_   │
│ PORT            │ │ (Postgres)   │ │ GRAPHQL_     │
│ DATABASE_URL ◄──┼─┤              │ │ ENDPOINT ◄───┤─ Backend URL
│ JWT_SECRET      │ │              │ │              │
│ GOOGLE_API_KEY  │ │              │ │              │
│ FRONTEND_URL ───┼──────────────┐ │ │              │
│                 │              │ │ │              │
└─────────────────┘              │ └──────────────┘
                                 │
                          Links to Frontend
```

---

## 🔌 DATA FLOW

```
┌──────────────────────────────────────────────────────┐
│                   USER BROWSER                        │
│              https://app.vercel.app                   │
│                  [React App]                          │
└────────────────┬─────────────────────────────────────┘
                 │
                 │ 1. User clicks "Login"
                 │
                 ↓
┌──────────────────────────────────────────────────────┐
│              VERCEL (Frontend)                        │
│                                                      │
│  React App                                          │
│  ├─ Shows Login Form                               │
│  └─ Sends: POST /graphql (mutation)                │
└────────────────┬──────────────────────────────────────┘
                 │
                 │ 2. GraphQL Query with credentials
                 │
                 ↓
┌──────────────────────────────────────────────────────┐
│              RENDER (Backend)                         │
│                                                      │
│  Apollo Server (Node.js)                            │
│  ├─ Receives GraphQL query                          │
│  ├─ Validates JWT token                            │
│  └─ Calls Prisma Client                            │
└────────────────┬──────────────────────────────────────┘
                 │
                 │ 3. Database Query (SQL)
                 │
                 ↓
┌──────────────────────────────────────────────────────┐
│            RENDER (Database)                         │
│                                                      │
│  PostgreSQL                                         │
│  ├─ Executes SQL                                    │
│  ├─ Fetches user record                            │
│  └─ Returns data                                    │
└────────────────┬──────────────────────────────────────┘
                 │
                 │ 4. Data back to Backend
                 │
                 ↓
┌──────────────────────────────────────────────────────┐
│              RENDER (Backend)                         │
│                                                      │
│  Apollo Server                                      │
│  ├─ Processes data                                 │
│  ├─ Creates JWT token                              │
│  └─ Sends GraphQL response                         │
└────────────────┬──────────────────────────────────────┘
                 │
                 │ 5. GraphQL Response + Token
                 │
                 ↓
┌──────────────────────────────────────────────────────┐
│              VERCEL (Frontend)                        │
│                                                      │
│  React App                                          │
│  ├─ Receives token                                 │
│  ├─ Saves to localStorage                          │
│  ├─ Redirects to Dashboard                         │
│  └─ Shows user data                                │
└────────────────┬──────────────────────────────────────┘
                 │
                 │ 6. Display in browser
                 │
                 ↓
┌──────────────────────────────────────────────────────┐
│                   USER BROWSER                        │
│              Dashboard Page Loaded                    │
│         ✅ User successfully logged in                │
└──────────────────────────────────────────────────────┘
```

---

## 🚀 DEPLOYMENT TIMELINE

```
0 min: You push to GitHub
  ↓
✅ 1 min: Both platforms detect push
  ├─ Render webhook triggered
  └─ Vercel webhook triggered
  ↓
✅ 2-3 min: Backend build starts
  ├─ npm install
  ├─ Prisma generate
  └─ Prisma migrate
  ↓
✅ 2-3 min: Frontend build starts
  ├─ npm install
  └─ npm run build
  ↓
✅ 5-6 min: Backend deploy starts
  ├─ Upload to server
  ├─ Start service
  └─ Health check
  ↓
✅ 3-4 min: Frontend deploy starts
  ├─ Upload to CDN
  ├─ Update DNS
  └─ SSL check
  ↓
✅ 7-8 min: BOTH LIVE! 🎉
  ├─ Backend: https://backend.onrender.com
  └─ Frontend: https://app.vercel.app
  ↓
Total time: ~8-10 MINUTES FOR FULL DEPLOYMENT
```

---

## 📱 USERS ACCESSING YOUR APP

```
┌─────────────────────────────────────────────────────┐
│  User opens browser                                  │
│  Types: https://app.vercel.app                      │
└────────────────┬────────────────────────────────────┘
                 │
                 ↓
        ┌────────────────┐
        │  Vercel CDN    │
        │ (Fast, global) │
        └────────┬───────┘
                 │
                 │ Browser downloads React app
                 │ (JS, CSS, HTML)
                 │
                 ↓
        ┌────────────────┐
        │ Browser shows  │
        │ React App      │
        └────────┬───────┘
                 │
                 │ When user clicks anything,
                 │ React makes GraphQL calls
                 │
                 ↓
        ┌────────────────┐
        │ Render Backend │
        │ Processes API  │
        └────────┬───────┘
                 │
                 │ Returns JSON data
                 │
                 ↓
        ┌────────────────┐
        │ React updates  │
        │ the page       │
        └────────────────┘
```

---

## ✅ FINAL DIAGRAM

```
                        GIT PUSH
                            │
                ┌───────────┴───────────┐
                │                       │
                ↓                       ↓
            🟡 RENDER              🔵 VERCEL
            ┌─────────┐            ┌─────────┐
            │ Backend │            │Frontend │
            │Database │            │React    │
            │Node.js  │            │CDN      │
            └────┬────┘            └────┬────┘
                 │                      │
                 └──────────┬───────────┘
                            │
                    ✅ LIVE ON INTERNET
                            │
                    Ready for Users!
```

---

## 🎯 KEY POINTS

1. **GitHub = Source of Truth**
   - One push triggers both deployments

2. **Render = Backend + Database**
   - All server logic
   - All data storage

3. **Vercel = Frontend**
   - User interface
   - Served globally from CDN

4. **They communicate via GraphQL**
   - Frontend sends queries
   - Backend processes and responds

5. **Everything is Automatic**
   - No manual deployment needed
   - No FTP or SSH required

---

**Ready to deploy? Read `DEPLOYMENT_TR.md` for step-by-step instructions!**
